{
  "name": "pi-langdock",
  "version": "1.0.0",
  "description": "Pi extension for Langdock AI models",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "postinstall": "npm run build",
    "check-upstream": "npx tsx scripts/check-upstream-models.ts"
  },
  "keywords": [
    "pi",
    "extension",
    "langdock",
    "ai"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "dependencies": {
    "typescript": "^5.0.0"
  },
  "pi": {
    "extensions": [
      "./dist/langdock.js",
      "./dist/ratelimit-footer.js"
    ]
  }
}
